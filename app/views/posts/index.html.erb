<h1>Listing posts</h1>

<table>
  <thead>
    <tr>
      <th>To</th>
      <th>Me</th>
      <th>date</th>
      <th>eventName</th>
      <th>freeSpace</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @posts.each do |post| %>
      <tr>
        <td><%= post.to %></td>
        <td><%= post.me %></td>
        <td><%= post.date %></td>
        <td><%= post.eventName %></td>
        <td><%= post.freeSpace %></td>
        <td><%= link_to 'Edit', edit_post_path(post) %></td>
        <td><%= link_to 'Destroy', post, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Post', new_post_path %>

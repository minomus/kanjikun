<%= form_for(@join) do |f| %>
  <% if @join.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@join.errors.count, "error") %> prohibited this join from being saved:</h2>

      <ul>
      <% @join.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :belong %><br>
    <%= f.text_field :belong %>
  </div>

  <div class="field">
    <%= f.label :event_id %><br>
    <%= f.collection_select(:event_id, Event.all, :id, :eventsName) %>
  </div>
    
   <div class="actions">
    <%= f.submit %>
  </div>

<% end %>
